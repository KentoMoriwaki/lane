# use-lane

**Promise-first. Transition-native.**

Lane is a minimal data layer for React 19. It keeps each keyed read's promise in
React state, so Suspense reads it, transitions replace it, and the screen you're
looking at stays live while the next data loads. No spinner flash, no torn UI,
no parallel query state machine.

React 19 ships the primitives to _render_ async data — `use(promise)` for data,
Suspense for loading, Error Boundaries for errors, transitions for non-blocking
updates, and `useOptimistic` for mutations. It doesn't ship the layer
underneath: stable promise identity by key, one shared request across components,
and a way to replace that promise after the source changes. **Lane is exactly
that layer — and nothing React already provides.**

```tsx
const { promise } = useLane(["user", id], ({ signal }) => fetchUser(id, signal));
const { data: user } = use(promise); // Suspense owns loading, Error Boundaries own errors
```

## Install

```sh
npm install use-lane
```

Requires React **19.2+** (`useEffectEvent`).

> 🤖 **Using an AI coding agent?** use-lane ships an
> [Agent Skill](https://github.com/KentoMoriwaki/lane/blob/main/packages/lane/README.md#agent-skill)
> — use-lane-aware guidance, version-locked to the installed package. Point your
> `AGENTS.md` / `CLAUDE.md` at `node_modules/use-lane/skills/use-lane/SKILL.md`.

## Why Lane

- **The promise is the state.** Lane keeps each key's current promise in React
  state. `use(promise)` is the only read path; Suspense and Error Boundaries own
  the first load and failure.
- **Promise replacement is transition-native.** Invalidation and revalidation
  replace the promise through a transition. Key changes compose with
  `startTransition` or `useDeferredValue`, keeping the current screen live.
- **No parallel state machine.** No `data` / `error` / `isLoading` result
  object. You read with `use(promise)`; Suspense and Error Boundaries do the
  rest.
- **No mutation helper, by design.** Mutations stay in React primitives, so
  optimistic UI lives next to the action that triggered it.
- **Minimal on purpose.** A typical `LaneProvider` + `useLane` import is about
  **3.1 kB** minified and Brotli-compressed because Lane does not reimplement
  state machinery React already provides.

## Learn more

- [API reference](/api-reference) — every export, option, and behavior.
- [Common mistakes](/common-mistakes) — anti-patterns, and the use-lane way to write them.
- [Supported architectures](/architectures) — RSC-first and RSC-seeded client
  ownership.
- [Design notes](/design-notes) — why Lane is shaped this way.
