node_modules
.next
dist
coverage
.turbo
test-results
playwright-report
.env
.env.local
*.log
*.tsbuildinfo
data

# Raw output of apps/activity-lab/scripts/owner-ask.mjs — the reading of a run
# belongs in OBSERVATIONS.md, the dump is scratch.
apps/activity-lab/.owner-ask-run.json

# Generated doc projection for the agent skill (docs/*.md is the source of truth).
# Published via the package's `files` allowlist, exactly like the git-ignored dist/.
packages/lane/skills/use-lane/references/

# Claude Code agent worktrees — local, ephemeral checkouts, not project content.
.claude/worktrees/
